[package]
name = "tinyvm-qjs"
version = "0.1.0"
edition = "2024"
rust-version = "1.97"
description = "Expression-subset qjs2wasm + eval_qjs over tinyvm eval_wasm (not agenterm-qjs / rquickjs)"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
tinyvm = { path = "../tinyvm" }
