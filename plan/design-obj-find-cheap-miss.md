# `__obj_find`：先看长度和两头的字，再问 `__str_eq`

| | |
|---|---|
| 日期 | 2026-08-31 |
| 状态 | 已落地：`runtime.rs` `obj_find`，`tests/obj_find_cost.rs` |
| 起因 | 下游 server-smoke 剖面（agenterm `plan/design-host-op-budget.md` §7）：`JSON.parse` 一个 20 键对象每成员 ≈2 500 步，其中 `__obj_set` 的线性键扫是大头 |

## 量

对象记录是线性表，`__obj_find` 从头走到答案，前面每一项都是一次未命中。此前每次未命中都进 `__str_eq`：
一次调用、比长度、再逐字节到第一个不同处——键同前缀时（`key000`…`key019`）**≈130 步一项**。
实测（进程内 `last_steps()` 差分）：20 键读最后一个 2 834、建 20 键字面量 27 623、100 键字面量 649 783、
`JSON.parse` 20 键对象每成员 2 533。

## 形状

不改记录布局。每一项先读它的键长度——长度不同就是整个未命中（**16 步**，原 28）；这一条最常见：真实记录的键长度各不相同。
长度相同才往下：同一指针直接命中（字面量键对字面量存的键，不读字节）；键 ≥ 4 字节时比首字、再比末字
（同一种写法造的键前缀相同、尾部不同——只比首字时 20 键反而更慢，31 143）；不足一字的键直接 `__str_eq`
（补位字节不承诺是什么，且最多三个字节）；三关都过才 `__str_eq` 逐字节。探针键只在前奏读长度，≥ 4 时再读首末两字。

**第一稿（同日早些）反而慢了 1%**：它先比指针、再比长度、给短键算掩码，前奏 ≈25 步、每项 ≈31 步——键长度各异时比原来那次 `__str_eq` 调用（28）还贵；
读是快了（记录五键 1 964 → 1 027），但建字面量 894 → 1 122、`JSON.parse` 一份 lease 答复 18 083 → 18 823，两条旅程各慢 0.8% / 1.4%（pin `428ea61`）。
长度在前、前奏只读短键用不到的东西，就是这一稿。

指针相等是命中不是「同一记录才算」：`o[1]` 与 `o["1"]` 仍由 `__str_eq` 判成同一属性（`the_cheap_miss_never_confuses_two_keys`
钉了不足一字的键、空键、首尾同中间异、计算键、数字键对字符串键、覆盖写）。

## 价

| | 前 | 第一稿 | 现在 |
|---|---|---|---|
| journal 记录五键各读一次 | 1 964 | 1 027 | **821** |
| journal 记录字面量 | 894 | 1 122 | 896 |
| lease 答复 `JSON.parse`（10 键，长度各异） | 18 083 | 18 823 | **17 815** |
| 20 键读最后一个 | 2 834 | 919 | **952** |
| 20 键读第一个 | 222 | 121 | 97 |
| 20 键字面量 | 27 623 | 10 203 | **10 533** |
| 100 键字面量 | 649 783 | 218 683 | 232 333 |
| `JSON.parse` 5 / 20 / 100 同形键，每成员 | 1 598 / 2 533 / 7 624 | 1 436 / 1 666 / 3 314 | 1 415 / 1 678 / 3 449 |

能持有对象的程序（`ctx.object_names` 那道门）**+119 B**；没有对象的程序保留旧体，`return 1;` 不动（10 198）。
未做：把签名存进条目（每项还能再省几步，但要动条目布局与所有写入点，小对象上前奏吃掉收益）；哈希表（要动记录语义）。
