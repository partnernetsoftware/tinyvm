# 研究轨：方法调用里的接收者绑定

一条轨的 Q 编号索引。状态只用四档：**已判决 / 进行中 / 已立项未开工 / 候选（未立项）**。
「候选」不得写结论，哪怕看起来显然。

**两条引用资格**（本仓踩过，见 `.claude/skills/decisive-experiment` §6）：

1. 没有 `RESULTS.md` 的 Q **不得**标「已判决」。
2. 标着「进行中」或「未开工」的 Q，它的数字**不得**被别处当作已决结论引用。

| Q | 问题（一句话） | 状态 | 结论 | 规格 | 实现 |
|---|---|---|---|---|---|
| Q1 | `"a".trim()` 里的 `trim` 怎么拿到 `"a"`？三种绑定机制哪种边际成本不随方法数增长？ | **进行中** | 未判决 | [`plan/design-method-binding-experiment.md`](../../plan/design-method-binding-experiment.md) | `crates/tinyvm-qjs` 的三个 default-off feature；变体 C 已实现 4 个方法里的 1 个 |

## Q1 一句话背景

`"ab".length` 已落地（`548fbbe`）。它是这条路上**唯一不需要绑接收者**的一格——
`length` 是值不是方法。其余全部卡在同一个问题上，三条候选：

- **A · `this`** —— 跟规范走。**带一笔债**：`this` 一落地，
  `plan/design-arrow-milestone.md` §1「箭头就是函数表达式」的等价立刻作废。
- **B · 绑定闭包** —— 属性读返回一个环境里装着接收者的函数值，复用闭包机制。
- **C · 调用点特化** —— `a.map(f)` 直接编成 `__arr_map(a, f)`，不产生函数值。

主判据是**斜率**（加第五个方法的边际字节 / 行数），不是截距。
主产出可能是判据 ⑥ 那张**泄漏清单**。

## 进度

- **变体 C：四个方法全部完成**，`method_conformance.rs` **7/7 且语料未改一个字**。
  - 判据 ① 布尔门**过**。
  - 判据 ② 布尔门**过**（七个无方法程序 Δ 全 0）。
  - 判据 ③ **= 0**：逐方法门控之后，加一个方法对不调用它的程序边际为 0。
    （第一次读成 +307，那是一刀切的门——**可修的实现细节**。当场落判就会
    错判一个变体，这是本轨迄今最值钱的教训。）
  - 判据 ③b（规格里原本没有，已补）：`trim` 44 / `indexOf` 48 / `push` 53 /
    **`map` 162** 字节每调用点。
  - 判据 ⑥ 泄漏 **六条**，最结构性的一条：**没有任何 runtime prefab 能
    `call_indirect`**，而 `map` 必须回调进函数值。C 靠调用点内联绕开，**A、B 绕不开**。
- **变体 A、B：完全未开工。只有一个变体的数字不构成比较，判决树一步都走不了。**

按引用资格第 2 条，Q1 状态是**进行中**，
**它的数字仍然不得被别处当作已决结论引用**。
