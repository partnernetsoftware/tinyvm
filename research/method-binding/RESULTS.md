# Q1 结果 —— **进行中，不得被当作已决结论引用**

按 `.claude/skills/decisive-experiment` §6 的引用资格：本 Q 状态是**进行中**，
下面每个数字都只对**变体 C 的一个方法**成立。任何「三条路线已判决」式的表格
**不得**引用本文件。

| | |
|---|---|
| 日期 | 2026-08-28 |
| 状态 | 进行中：**C、B 两个变体四个方法全完成，判据 ① 各 7/7**；**A 未开工**，因此仍**无法判决** |
| 规格 | [`plan/design-method-binding-experiment.md`](../../plan/design-method-binding-experiment.md) |

## 判据 ①（布尔门）：变体 C **过**

`method_conformance.rs` **C 7/7、B 7/7**，两边**语料都一个字未改**——这正是那份语料在任何变体
存在之前就写好的目的。四个方法全部跑通：`trim`（含 NBSP / ZWNBSP / 非 ASCII）、
`indexOf`（含 UTF-16 位置、空针、多码元针）、`push`（含通过第二个名字和通过函数
参数看见的改动）、`map`（含箭头、具名函数、**捕获外层绑定的闭包**、链式 `map`）。
三条不变量也全绿。

## 度量条件

- **边界**：整个 `.wasm` 产物——机制码、方法体、数据段全在内。
- **工具**：`compile_qjs_m1(src).len()`，全轨一把尺，**不跨工具相除**。
- **构建**：guest wasm。`no_std` / `panic=abort` 那条轴在这里**不适用**，
  这些数**不得**与 host 侧二进制的字节并排。
- **目标/执行**：wasm32，且下表每个程序都被 `method_conformance.rs` **真跑过**，
  不是只量不跑的产物。
- **档位**：全部是 **L3**。**L1（纯机制）与 L2（机制+方法体）未测定**，
  拆开需要逐函数尺寸；下面任何一个数都不得被当成 L1 或 L2。
- **基线**：同一份源码、同一个提交，只切 `--features method-callsite`。

复跑：

```sh
cargo test -p tinyvm-qjs --features method-callsite \
  --test method_measure -- --nocapture | grep '^SIZE'
cargo test -p tinyvm-qjs --test method_measure -- --nocapture | grep '^SIZE'
```

## 判据 ②（布尔门）：**过**

无方法调用的程序必须 Δ = 0。七个程序，全部 0：

| 程序 | off | on | Δ |
|---|---|---|---|
| `return 1;` | 9765 | 9765 | **0** |
| 字符串拼接 | 9817 | 9817 | **0** |
| 对象 | 9854 | 9854 | **0** |
| 数组 | 10726 | 10726 | **0** |
| 闭包 | 10032 | 10032 | **0** |
| JSON | 15420 | 15420 | **0** |
| `"ab".length` | 9906 | 9906 | **0** |

## 判据 ⑤（截距）与 ③b（**每调用点**的斜率）

| 程序 | off | on | Δ |
|---|---|---|---|
| `trim()` × 1 | 9828 | 10256 | **428** |
| × 2 | 9887 | 10359 | **472** |
| × 3 | 9949 | 10463 | **514** |
| `indexOf()` × 1 | 9841 | 10205 | **364** |
| × 2 | 9911 | 10323 | **412** |
| `push()` × 1 | 10614 | 10725 | **111** |
| × 2 | 10717 | 10881 | **164** |
| `map()` × 1 | 10723 | 10901 | **178** |
| × 2 | 10909 | 11249 | **340** |
| `trim()` + `indexOf()` | 10054 | 10838 | **784** |
| `o.trim()`（普通对象接收者） | 9941 | 10370 | **429** |

**③b，每多一个调用点：**

| 方法 | 字节/调用点 |
|---|---|
| `trim` | 44 |
| `indexOf` | 48 |
| `push` | 53 |
| **`map`** | **162** |

前三个一致在 43–53，说明那部分代价属于**机制**（接收者测试 + 两个 `If` + 暂存）
而不是方法。`map` 是 **3 倍**，因为它**没有 prefab**：变体 C 在**每一个调用点上
内联整个循环**。理由见泄漏 4——这是 C 为了绕开「prefab 不能 `call_indirect`」
所付的价，而 A、B 绕不开。

（`push` 的截距只有 111，是因为它的 off 基线里已经有数组集合了——`push1` 写了数组
字面量。跨行比截距要先看 off 列。）

## 判据 ③（主判据，加一个**方法**的边际）：**先读成陡，做了逐方法门控后是 0**

**第一次读数（整集合一刀切的门）**：实现第二个方法 `indexOf` 之后，重测**同一个只
调用 `trim()` 的程序**：

| 程序 | 集合里 1 个方法 | 集合里 2 个方法（一刀切） | 变化 |
|---|---|---|---|
| `return "  a  ".trim();` | Δ **428** | Δ **735** | **+307** |

一个只写 `trim()` 的程序，为它永远不会调用的 `indexOf` 多付 307 字节。
按 §4 判决树第 1 条，这是「线性且陡」，本该判 C 负。

**但没有落判决，因为一刀切是本实现的选择、不是变体 C 的定义。**
做了**逐方法门控**（`method::Plan`：调用点要哪个就发哪个，helper 按依赖拉入）后重测：

| 程序 | 逐方法门控后的 Δ |
|---|---|
| 只用 `trim` | **428**（回到只有一个方法时的数） |
| 只用 `indexOf` | **364** |
| 两个都用 | **784** |

**加第五个方法，对不调用它的程序边际是 0。** 判据 ③ 因此**不判 C 负**。
（428 + 364 = 792，实测 784，8 字节的差在 LEB128 宽度与字面量上；
两个方法之间**没有**共享成本，符合「各付各的」。）

**这次修改本身的代价要算在 C 头上**：`method::Plan` 是 **27 行**（非空非注释），
而且它存在的理由是函数索引由**位置**决定——抽掉一个成员后面全挪，
正是 `plan/design-string-length-milestone.md` §3 里 `Rt::Len` 那个问题的第二次相遇。
**A、B 不需要这 27 行**：它们的方法是函数值，只有被引用的那个才需要存在。

**方法论上的一条**：第一次读数如果当场落判，就会用一个**可修的实现细节**
判掉一个变体。§4 判决树该在「这个数是变体的性质还是这次实现的性质」问清楚之后才走。
这条已写进本文件，也是这条轨迄今最值钱的一次教训。

## 判据 ⑥ 泄漏清单（变体 C，六条）

1. **运行期接收者测试省不掉。** 源码写 `x.trim()`，`x` 是 String 还是带 `trim`
   属性的对象要到运行时才知道，而
   `method_conformance::a_plain_object_property_named_like_a_method_is_untouched`
   就是「第二种必须照常工作」的断言。所以 C **省掉的是函数值，不是分派**——
   分支只是从被调用者的值里搬到了每一个调用点上。这就是那 43–53 字节。
2. **接收者只能求值一次**，所以非快路那条**不能**退回去调现成的 `indirect_call`
   ——那会重新降级 callee，`f().trim()` 会把 `f` 调两次。于是必须有暂存对 +
   两个 `If` 的形状。
3. **IR 没有 `Else`，`BlockType` 只有 `Empty`**，所以「产生一个值的分支」必须
   经由局部变量绕一圈。IR 的性质，但**付钱的是变体**。
4. **`map` 没有 prefab，只能在调用点内联。** 全仓**没有任何 runtime prefab 能
   `call_indirect`**（唯一一处在 `emit.rs`），而 `map` 必须回调进函数值。
   C 靠「在调用点内联」绕开了，代价是 ③b 三倍。**A、B 绕不开**：它们的方法**是**
   函数值，方法体就是函数，那个函数需要的正是 C 避开的那个能力。
   **这是本轨迄今最结构性的一条发现。**
5. **跨门依赖。** `__m_push` 要靠 `__arr_push`，那在**数组**集合、**数组**门后面。
   于是一个 `push` 调用点必须伸手把另一个不相干的门打开。A、B 的方法体一样依赖它，
   但不需要**编译器**知道——它们经由值到达，而不是经由这个模块要算出来的索引。
6. **接收者测试是逐方法的。** `trim` 要 String、`push` 要 Array，所以调用点上的
   类型测试不是一个共享检查，而是每个方法一份。小，但扛着它的是调用点。

另记两条**不是**泄漏、但影响读数的事：

- `objtrim` 花 429 字节却一次也到不了 prefab——**门不精确**，也不可能精确（同泄漏 1）。
- `ws_width` 只认 ECMA-262 12.2/12.3 里**具名的十个**，不认 `Zs` 其余
  （U+1680、U+2000..200A、U+202F、U+205F、U+3000）。**已知缺口**，三变体同价，
  不影响比较；方法体若被扶正必须先补。

## B vs C 同口径对比 —— **四个方法全齐**

| 方法 | C 截距 | C 每调用点 | B 截距 | B 每调用点 | 交叉点 |
|---|---|---|---|---|---|
| `trim` | 428 | **43** | 672 | **32** | 约 22 个调用点 |
| `indexOf` | 364 | **48** | 607 | **27** | 约 12 个 |
| `push` | 111 | **53** | 331 | **27** | 约 9 个 |
| **`map`** | 178 | **162** | 391 | **30** | 约 **2–3 个** |

（原始数字见文末复跑命令；`push` 的截距小是因为它的 off 基线里已经有数组集合了。）

**结构上的结论，四个方法一致地指向同一件事：**

- **B 的斜率与方法无关**，四个方法都是 **27–32**。因为 B 每个调用点做的事永远
  一样：读一个属性、间接调用一次。方法体在 prefab 里只存一份。
- **C 的斜率随方法变**：三个 prefab 方法 43–53，`map` **162**。因为 C 每个调用点
  要做的是「类型测试 + 两个 `If` + 这个方法自己的调用形状」，而 `map` 那个形状
  是**一整个循环**。

§3 钉死「**斜率优先于截距**」。**四个方法里 B 的斜率都更低**，`map` 上低 5.4 倍。
判据 ⑦（`let m = x.trim; m()`）也是 B 行、C 不行。

**这仍然不是判决**：变体 A 一行未写。三选一的题不能用两个变体的数落判。

### 泄漏 4 的前提被推翻了一半

原记「**没有任何 runtime prefab 能 `call_indirect`**，所以 A、B 绕不开」。
实测：**能**——把统一签名的 type index 交给 prefab 层就行。
所以那不是「B 做不到」，而是「B 要先付一笔入场费」，费用清单在泄漏 5–8。

### B 的门被修过**两次**，都是同一个检查救的

1. 第一版把臂挂在 `obj_get` 的 String 分支（由 `.length` 控制）：
   `return "ab".length;` 平白多付 **514** 字节，违反 §1.2。分开门后回到 0。
2. 第二版 B 的 surface 恒含 `__m_map_bound`，而它要调数组集合——
   于是只用 `trim` 的程序也把数组集合拖进来。按**读到的名字**分别门控后回到 0。

**同一个检查（§2.6 可修性检查）在这条轨上已经救下三次误判。**
已写进 `.claude/skills/decisive-experiment` §2.6。

## 判据 ⑥ 泄漏清单：变体 B（四条）

1. **B 打开闭包机制。** 绑定后的方法是个函数记录，而记录只有在程序有闭包时才带
   环境槽。所以一个**没有任何闭包**的程序，用了 B 的方法就要把整套闭包机制打开。
   C 不付这笔。
2. **B 需要函数表和统一签名。** 绑定方法是个函数值，所以程序哪怕自己一个函数值
   都没有，也要有表。C 不付这笔。
3. **B 有第二套环境布局。** 闭包的环境是 **cell** 的向量（绑定可以被写回）；
   绑定的接收者写不回（23.1.3 的方法不重绑 `this`），所以用了 12 字节裸对而不是
   cell——省一层间接，但引擎里多了一种形状。
4. **B 的门比 C 粗。** C 按**调用点**开门（`trim()` 被调用才开）；
   B 只能按**属性读**开门（读到 `trim` 这个名字就开），因为元素索引被烧进
   `obj_get`，而那是在谁也不知道源码里有没有 `trim` 之前发射的。
5. **B 要求 prefab 能 `call_indirect`**——把统一签名的 type index 交给 prefab 层。
   这是**入场费**，C 不付。（也正是它把泄漏 4 的前提推翻了一半。）
6. **B 抬高了统一签名的最小参数个数。** `map` 的 adapter 要转发一个 JS 值，
   所以哪怕程序里一个调用都没有，统一签名也得有一个值参数。**B 抬的是整张表
   共享的签名**，C 碰都不碰它。
7. **B 迫使类型段换个顺序生成。** prefab 是在统一签名被 intern **之前**建的，
   而 `__m_map_bound` 需要那个 index，所以 B 必须把统一签名提前——
   一个全局布局决定被一个变体改了方向。
8. **B 的跨门依赖比 C 广。** 读一个 `map` 就把**数组门**拉开（`__m_map_bound`
   要 `__arr_new`/`__arr_get`/`__arr_push`），这一条和 C 的 `push` 同类，
   但 C 的是「调用了才拉」，B 的是「读到名字就拉」。

**B 的一条结构性优势**：它的接收者测试**搭了顺风车**——`obj_get` 的 String 分支
本来就存在（`.length` 要它），B 的臂长在里面。C 的测试在调用点上，那里本来没有分支，
所以那 43 字节是**新开销**。这正是 B 斜率更低的原因。

## 还没有的东西 —— **所以本 Q 仍不能判决**

- **变体 A（`this`）：完全未开工。** 这是唯一还挡着判决的东西。
- 判据 ④（边际改动行数）：**未测定**。
- **L1 / L2 分档：未测定**。上面每个数都是 **L3**。
- 加**第五个**方法的实测边际（判据 ③ 的正式做法）：未做。
  当前 ③ = 0 的结论（C）是从「加第二个方法后重测第一个」推出来的，
  方向对，但不是规格写的那个实验；B 的 ③ 未测。

### 目前的态势，说清楚但不当判决

四个方法上 **B 的斜率都低于 C**，`map` 上低 5.4 倍，而且 B 的斜率**与方法无关**
（27–32），C 的**随方法变**（43–162）。判据 ⑦ 也是 B 赢。
若 A 给不出更好的斜率，判决树第 1 条会指向 B。

**但 A 一行都还没写，而 A 恰恰是「跟规范走」的那条**——它是唯一能顺手把
`this` 带进来的方案，而 `this` 本身是另一批能力（方法、`class`、`bind`）的前提。
A 的账里有一笔别人没有的债（`design-arrow-milestone.md` §1 的等价作废），
也可能有一笔别人没有的收益。**没量之前不猜。**

### 对下一个人的三条

1. **变体 A 的第一件事**不是写 `this`，是量那笔债——它是 A 独有的成本。
2. 判据 ④ 和 L1/L2 分档还空着；③ 的正式做法（加第五个方法）也还没做。
3. **判据第一次难看时，先做 §2.6 的可修性检查。** 本轨已因此三次避免误判。

## 复跑

```sh
cargo test -p tinyvm-qjs --test method_measure -- --nocapture | grep '^SIZE'
cargo test -p tinyvm-qjs --features method-callsite --test method_measure -- --nocapture | grep '^SIZE'
cargo test -p tinyvm-qjs --features method-bound     --test method_measure -- --nocapture | grep '^SIZE'
cargo test -p tinyvm-qjs --features method-callsite --test method_conformance
cargo test -p tinyvm-qjs --features method-bound     --test method_conformance
```
