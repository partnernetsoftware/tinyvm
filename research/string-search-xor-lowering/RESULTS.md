# String search XOR-lowering results

Status: **pending**.

Run the frozen court only after the baseline specification is committed. This
file will record X0-X5, raw totals, exact identities, emitted module sizes,
decision trace and deviations.
